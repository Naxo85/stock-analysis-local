/*
 * Disabled remote backup file.
 *
 * The Apps Script project used to contain "Copia de Value_refresh_1m.js" with
 * duplicate global helper functions. Those duplicates can override the active
 * Value_refresh_1m.js helpers depending on Apps Script load order.
 *
 * Keep this file empty on purpose when pushing to the remote backup filename.
 */
